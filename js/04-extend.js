jQuery.extend = jQuery.fn.extend = function() {
	//1、 定义变量 
	
	//2、判断是否是深拷贝
	if() {}
	
	//3、判断参数是否正确
	if() {}
	
	//4、判断是否是插件
	if() {}
	
	//5、处理一个或多个对象
	for() {
		
		if() {} //防止循环引用
		
		if() {} // 深拷贝
		else if() {} //浅拷贝
	}
	
	
}
